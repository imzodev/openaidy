import { Layout } from './Layout';

export function WebhooksPage() {
  return (
    <Layout title="Webhooks" description="HTTP endpoint triggers">
      <div class="flex items-center justify-center h-64">
        <p class="text-text-tertiary">Webhook management coming soon</p>
      </div>
    </Layout>
  );
}
