import { Layout } from './Layout';

export function TasksPage() {
  return (
    <Layout title="Tasks" description="Kanban view of tasks">
      <div class="flex items-center justify-center h-64">
        <p class="text-text-tertiary">Tasks kanban view coming soon</p>
      </div>
    </Layout>
  );
}
